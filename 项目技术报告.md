# Learning Tracker 项目技术报告

## 1. 项目概述与功能列表

### 1.1 项目简介

Learning Tracker 是一个基于 Vue 3 + TypeScript 构建的**学习进度追踪工具**，采用单页应用（SPA）架构，专注于帮助用户管理学习计划、追踪任务完成进度，并通过集成 LLM（大语言模型）实现智能化的学后测试功能。

### 1.2 核心功能列表

| 功能模块 | 描述 | 对应组件/文件 |
|---------|------|-------------|
| **学习计划管理** | 支持多计划导入、切换、删除 | `PlanManager.vue`, `ImportModal.vue` |
| **任务状态追踪** | 任务完成标记、状态分类（必看/倍速/跳过） | `VideoTask.vue`, `ChapterSection.vue` |
| **进度统计面板** | 实时展示完成百分比、必看视频数、重点内容数 | `StatsPanel.vue` |
| **智能学习建议** | 基于完成进度动态生成个性化学习建议 | `App.vue` (learningTip computed) |
| **星级自我评价** | 完成任务后进行 1-5 星自我评分 | `StarRating.vue`, `VideoTask.vue` |
| **智能筛选** | 按任务状态筛选显示 | `FilterBar.vue` |
| **学后测试** | 基于 LLM 生成个性化测试题，支持答题、结果分析、历史记录 | `TestModal.vue` |
| **LLM 配置管理** | 支持 8 家 LLM 提供商（含自定义），一键连接测试 | `LLMConfigModal.vue` |
| **暗色/亮色主题** | 双主题切换，CSS 变量驱动 | `main.css`, `App.vue` |
| **本地数据持久化** | 所有数据存储在浏览器 localStorage | `planStore.ts` |

### 1.3 支持的 LLM 提供商

| 提供商 | 预设 API 地址 | 预设模型 |
|--------|-------------|---------|
| DeepSeek | `api.deepseek.com` | deepseek-chat, deepseek-reasoner |
| 智谱 AI (GLM) | `open.bigmodel.cn` | glm-4-plus, glm-4-flash |
| 通义千问 | `dashscope.aliyuncs.com` | qwen-plus, qwen-turbo |
| Moonshot | `api.moonshot.cn` | moonshot-v1-128k/32k/8k |
| 零一万物 (Yi) | `api.lingyiwanwu.com` | yi-lightning, yi-large |
| 豆包 | `ark.cn-beijing.volces.com` | doubao-pro-32k/128k |
| OpenAI | `api.openai.com` | gpt-4o, gpt-4, gpt-3.5-turbo |
| 自定义 | 用户输入 | 用户输入 |

---

## 2. 技术架构分析

### 2.1 技术栈总览

```
前端框架:    Vue 3.5.13 (Composition API + <script setup>)
语言:        TypeScript 5.6
构建工具:    Vite 6.2
状态管理:    Pinia 2.3
工具库:      @vueuse/core 12.0
包管理:      npm
```

### 2.2 架构模式

**单页应用（SPA）+ 模块化组件架构**

```
┌─────────────────────────────────────────┐
│              index.html                 │
│         (单页入口，挂载 #app)             │
└─────────────────┬───────────────────────┘
                  ▼
┌─────────────────────────────────────────┐
│              main.ts                    │
│    Vue 实例创建 → Pinia 注册 → 挂载 App  │
└─────────────────┬───────────────────────┘
                  ▼
┌─────────────────────────────────────────┐
│              App.vue (根组件)            │
│  状态协调 + 模态框管理 + 动态学习建议计算   │
└─────────────────┬───────────────────────┘
                  ▼
        ┌─────────────────┐
        │   Pinia Store   │
        │  (planStore.ts) │
        │  全局状态 + 持久化 │
        └────────┬────────┘
                 ▼
    ┌────────────────────────────┐
    │      业务组件层 (12个)        │
    │  Header │ Stats │ Filter   │
    │  Chapter│ Task  │ Star     │
    │  Modal x6 (Import/Config/  │
    │  Test/Plan/Confirm/Tip)    │
    └────────────────────────────┘
```

### 2.3 状态管理设计

**Pinia Store (`planStore.ts`)** 采用 Composition API 风格：

| 状态类别 | 具体状态 | 持久化方式 |
|---------|---------|-----------|
| 核心数据 | `plans`, `activePlanId` | `useLocalStorage` (自动) |
| UI 偏好 | `theme` | `useLocalStorage` (自动) |
| LLM 配置 | `llmConfig` | 手动 `localStorage` + `watch` |
| 测试历史 | `testHistory` | `useLocalStorage` (自动) |

**设计决策**：LLM 配置使用手动 `watch` + `localStorage` 而非 `useLocalStorage`，是为了在初始化时进行数据格式验证，确保存储的 API Key 等敏感数据在解析异常时有容错处理。

### 2.4 样式架构

采用 **CSS 变量驱动 + 单文件全局样式** 的设计：

- **设计系统**：`main.css` 定义完整的 Design System（颜色、间距、圆角、阴影、动画）
- **主题切换**：通过 `data-theme` 属性切换暗色/亮色两套 CSS 变量
- **组件样式**：大部分组件使用 `scoped` 样式，但依赖全局 CSS 变量
- **设计哲学**："Warm restraint" — 炭黑与琥珀金配色，无装饰性噪点，零渐变文字

---

## 3. 代码结构统计

### 3.1 文件统计（src 目录）

| 类别 | 文件数 | 代码行数 | 占比 |
|------|--------|---------|------|
| Vue 组件 (`.vue`) | 14 | 3,077 | 55.8% |
| TypeScript (`.ts`) | 4 | 325 | 5.9% |
| CSS 样式 (`.css`) | 1 | 1,045 | 18.9% |
| 静态资源 | 3 | - | - |
| **合计** | **22** | **~4,447** | **100%** |

### 3.2 各文件行数明细

| 文件路径 | 行数 | 类型 | 职责 |
|---------|------|------|------|
| `src/components/TestModal.vue` | 1,277 | Vue | 学后测试（最复杂组件，含5种视图模式） |
| `src/styles/main.css` | 1,045 | CSS | 全局设计系统 + 组件基础样式 |
| `src/components/LLMConfigModal.vue` | 481 | Vue | LLM 配置管理（8家提供商预设） |
| `src/App.vue` | 303 | Vue | 根组件，状态协调与动态建议 |
| `src/components/ChapterSection.vue` | 248 | Vue | 章节展示、进度条、测试入口 |
| `src/stores/planStore.ts` | 217 | TS | Pinia Store，核心业务逻辑 |
| `src/components/VideoTask.vue` | 189 | Vue | 单个任务项（复选框、状态标签、评分） |
| `src/components/ImportModal.vue` | 167 | Vue | JSON 计划导入 |
| `src/components/PlanManager.vue` | 100 | Vue | 计划列表管理 |
| `src/types/index.ts` | 91 | TS | 全项目 TypeScript 类型定义 |
| `src/components/StarRating.vue` | 72 | Vue | 五星评分组件 |
| `src/components/AppHeader.vue` | 66 | Vue | 顶部导航栏 |
| `src/components/StatsPanel.vue` | 53 | Vue | 统计面板（4项指标） |
| `src/components/ConfirmModal.vue` | 40 | Vue | 确认删除弹窗 |
| `src/components/FilterBar.vue` | 37 | Vue | 状态筛选栏 |
| `src/components/EmptyState.vue` | 24 | Vue | 空状态引导 |
| `src/components/TipCard.vue` | 20 | Vue | 学习建议卡片 |
| `src/main.ts` | 10 | TS | 应用入口 |
| `src/vite-env.d.ts` | 7 | TS | Vite 类型声明 |

### 3.3 依赖分析

**生产依赖（4个）**：
- `vue@3.5.13` — 核心框架
- `pinia@2.3.0` — 状态管理
- `@vueuse/core@12.0.0` — 工具函数（主要使用 `useLocalStorage`）

**开发依赖（4个）**：
- `vite@6.2.0` — 构建工具
- `@vitejs/plugin-vue@5.2.3` — Vue SFC 支持
- `typescript@~5.6.0` — 类型系统
- `vue-tsc@2.2.8` — Vue 类型检查

**依赖特点**：极度精简，无 UI 组件库、无路由库、无 HTTP 客户端（直接使用原生 `fetch`）、无测试框架。

---

## 4. 数据流分析

### 4.1 数据模型

```
AppData
├── plans: Plan[]
│   └── Plan
│       ├── id, name, createdAt
│       └── chapters: Chapter[]
│           └── Chapter
│               ├── id, title, dayLabel
│               └── tasks: Task[]
│                   └── Task
│                       ├── id, title, videoDuration, status
│                       ├── chapter, important, completed
│                       └── rating?: number
├── activePlanId: string | null
├── theme: 'dark' | 'light'
└── llmConfig?: LLMConfig
    └── { provider, apiKey, apiUrl, model }

TestHistoryRecord
├── id, chapterId, chapterTitle, planId, planName
├── score, totalQuestions, scorePercentage, timeSpent
├── answers: TestAnswerRecord[]
└── createdAt
```

### 4.2 核心数据流

```
用户操作 → 组件 emit 事件 → App.vue 事件处理 → 
Pinia Store Action → 修改响应式状态 → 
useLocalStorage 自动持久化 → localStorage
         ↓
    Vue 响应式系统 → 组件自动重渲染
```

### 4.3 关键数据流场景

**场景 1：导入学习计划**
```
用户点击"导入计划" → ImportModal 打开
  → 用户填写名称 + 粘贴 JSON
  → 点击"导入" → emit('import', name, tasks)
  → App.vue handleImport() → store.importPlan()
  → store 内部：按 chapter 分组 → 生成 ID → 构建 Plan 对象
  → plans.value.push(newPlan) → activePlanId 更新
  → useLocalStorage 自动持久化
  → App.vue hasPlan computed 变化 → 显示主内容
```

**场景 2：完成任务并评分**
```
用户点击任务行 → VideoTask emit('toggle', taskId)
  → App.vue handleToggleTask() → store.toggleTask()
  → store 查找任务 → task.completed = !task.completed
  → 如标记为未完成 → task.rating = undefined
  → 自动持久化
  → VideoTask 条件渲染评分区域 (v-if="task.completed")
  → 用户点击星星 → emit('rate', taskId, rating)
  → store.rateTask() → task.rating = rating
```

**场景 3：LLM 生成测试题**
```
用户点击"测试"按钮 → ChapterSection emit('openTest', chapter)
  → App.vue 设置 testChapter + showTestModal
  → TestModal 打开，watch 触发 → generateQuestions()
  → 构建 Prompt（含知识点清单 + 自评星级信息）
  → fetch() 调用 LLM API（含 AbortController 支持取消）
  → 三级 JSON 解析容错（直接解析 → 代码块提取 → 正则匹配）
  → 题目数据校验（过滤无效题目，越界校验）
  → 初始化答案数组 → 启动计时器 → 进入答题模式
  → 用户答题 → 确认答案 → 计分 → 下一题/结果
  → 最后一题完成 → 自动保存测试历史到 store
```

### 4.4 数据持久化策略

| 数据 | 存储 Key | 持久化方式 | 说明 |
|------|---------|-----------|------|
| 计划列表 | `learning-tracker-data-plans` | `useLocalStorage` | 自动响应式同步 |
| 当前计划 ID | `learning-tracker-data-activePlanId` | `useLocalStorage` | 自动响应式同步 |
| 主题 | `learning-tracker-data-theme` | `useLocalStorage` | 自动响应式同步 |
| LLM 配置 | `learning-tracker-data-llm-config` | 手动 `watch` + `localStorage` | 初始化时格式验证 |
| 测试历史 | `learning-tracker-data-test-history` | `useLocalStorage` | 自动响应式同步 |

---

## 5. 部署状态

### 5.1 构建配置

**Vite 配置 (`vite.config.ts`)**：
- `base: '/learning-tracker/'` — 子路径部署（适配 GitHub Pages）
- 路径别名 `@` → `src/`
- 仅使用 `@vitejs/plugin-vue` 一个插件

**TypeScript 配置 (`tsconfig.json`)**：
- `target: es2023`, `module: esnext`
- `moduleResolution: bundler`
- `verbatimModuleSyntax: true` — 强制显式类型导入
- `noUnusedLocals: true`, `noUnusedParameters: true` — 严格未使用检查
- `erasableSyntaxOnly: true` — 仅使用可擦除语法

### 5.2 CI/CD 流水线

**GitHub Actions 工作流** (`.github/workflows/deploy.yml`)：

| 阶段 | 说明 |
|------|------|
| 触发条件 | `push` 到 `main` 分支 或 `workflow_dispatch` 手动触发 |
| 构建环境 | `ubuntu-latest`, Node.js 20 |
| 构建步骤 | checkout → setup-node → `npm ci` → `npm run build` |
| 部署目标 | GitHub Pages（通过 `actions/deploy-pages@v4`） |
| 产物路径 | `./dist` |

### 5.3 部署状态评估

| 维度 | 状态 | 说明 |
|------|------|------|
| 自动部署 | 已配置 | push 到 main 自动触发 |
| 部署目标 | GitHub Pages | 静态站点托管 |
| 构建产物 | 纯静态文件 | 无服务端依赖 |
| 自定义域名 | 未配置 | 使用默认 `*.github.io` 域名 |
| 环境隔离 | 无 | 仅生产环境，无 staging |
| 回滚机制 | 手动 | 依赖 Git 回退 + 重新部署 |

### 5.4 部署限制

- **纯前端应用**：无后端服务，所有 LLM API 调用直接从浏览器发起（存在 CORS 风险）
- **数据本地存储**：用户数据仅存于浏览器 localStorage，换设备/清缓存即丢失
- **无用户认证**：无登录系统，计划数据无法跨设备同步

---

## 6. 问题与建议

### 6.1 已知问题汇总（基于代码审查与 Dogfood 报告）

| 编号 | 问题 | 严重程度 | 类别 |
|------|------|---------|------|
| ISSUE-005 | 错误提示使用 `alert()`，打断用户流程 | **High** | UX |
| ISSUE-006 | 导入操作缺少加载状态指示器 | **High** | UX |
| ISSUE-001 | 部分 SVG 图标缺少 `aria-label` | Medium | 可访问性 |
| ISSUE-002 | 表单输入框 `label` 未使用 `for` 关联 | Medium | 可访问性 |
| ISSUE-003 | `--text-muted` 颜色对比度可能不足 | Medium | 可访问性 |
| ISSUE-004 | 部分交互元素缺少键盘焦点指示器 | Medium | 可访问性 |
| ISSUE-007 | 缺少 PWA 支持 | Low | 最佳实践 |
| ISSUE-008 | 缺少 SEO 元标签 | Low | SEO |
| ISSUE-009 | API Key 以明文存储在 localStorage | Low | 安全 |
| ISSUE-010 | 缺少单元测试 | Low | 质量 |
| ISSUE-011 | 极小屏幕响应式布局优化空间 | Low | 视觉 |

### 6.2 架构层面建议

#### 建议 1：引入 Toast 通知系统替代 `alert()`
**问题**：`ImportModal.vue` (第65、69、86行) 和 `LLMConfigModal.vue` (第135、167、171行) 使用 `alert()` 进行表单验证提示。

**方案**：
- 创建 `ToastNotification.vue` 组件
- 或使用 Pinia 管理全局 Toast 状态
- 在表单字段下方显示内联错误信息

#### 建议 2：拆分 `TestModal.vue`（1,277行）
**问题**：该组件是目前最大的文件，包含 5 种视图模式（loading/answering/result/detail/history），职责过重。

**方案**：
```
TestModal.vue          # 壳组件，负责视图切换
├── TestLoading.vue    # 加载中
├── TestAnswering.vue  # 答题界面
├── TestResult.vue     # 结果展示
├── TestDetail.vue     # 答题详情
└── TestHistory.vue    # 历史记录
```

#### 建议 3：提取 LLM API 调用层
**问题**：`TestModal.vue` 中直接调用 `fetch()` 与 LLM 交互，逻辑与 UI 耦合。

**方案**：创建 `src/services/llmService.ts`：
```typescript
// 封装 fetch 调用、错误处理、JSON 解析容错
export async function generateQuestions(config: LLMConfig, chapter: Chapter): Promise<TestQuestion[]>
export async function testConnection(config: LLMConfig): Promise<boolean>
```

#### 建议 4：添加单元测试
**问题**：项目无任何测试覆盖，核心业务逻辑（`planStore.ts` 中的数据处理）缺乏保障。

**方案**：
- 添加 `vitest` + `@vue/test-utils`
- 优先测试 `planStore` 的 `importPlan`、`toggleTask`、`rateTask`
- 测试 `TestModal` 的答题计分逻辑

#### 建议 5：考虑数据导出/导入增强
**问题**：当前仅支持 JSON 导入，数据无法导出备份，换设备即丢失。

**方案**：
- 添加"导出计划"功能（JSON 下载）
- 或考虑集成云存储（如 GitHub Gist、Firebase 等）

#### 建议 6：API Key 安全存储
**问题**：LLM API Key 以明文存储在 localStorage。

**方案**：
- 使用 `SubtleCrypto` API 进行简单加密（密钥可派生自用户输入的密码）
- 或每次会话要求重新输入（牺牲便利性换取安全性）

### 6.3 性能优化建议

| 建议 | 说明 |
|------|------|
| 代码分割 | `TestModal` 可改为异步组件 `defineAsyncComponent`，减少首屏加载 |
| 图片优化 | `hero.png` 未使用（58行代码引用但实际未在组件中引入），建议清理 |
| CSS 优化 | `main.css` 包含大量未使用的动画关键帧，可通过 PurgeCSS 清理 |

### 6.4 可访问性优化建议

| 建议 | 优先级 |
|------|--------|
| 为所有功能性按钮添加 `aria-label` | 高 |
| 表单 `label` 添加 `for` 属性关联输入框 | 高 |
| 提高 `--text-muted` 不透明度至 0.5+ | 中 |
| 为模态框添加 `role="dialog"` 和 `aria-modal="true"` | 中 |
| 实现焦点陷阱（Focus Trap）确保键盘用户在模态框内循环 | 中 |

---

## 附录：关键文件索引

| 文件 | 路径 |
|------|------|
| 应用入口 | `src/main.ts` |
| 根组件 | `src/App.vue` |
| 类型定义 | `src/types/index.ts` |
| 状态管理 | `src/stores/planStore.ts` |
| 全局样式 | `src/styles/main.css` |
| 测试组件 | `src/components/TestModal.vue` |
| LLM 配置 | `src/components/LLMConfigModal.vue` |
| 构建配置 | `vite.config.ts` |
| TS 配置 | `tsconfig.json` |
| 部署配置 | `.github/workflows/deploy.yml` |

---

*报告基于对 `src` 目录下全部 19 个源代码文件的完整读取与分析生成。*
